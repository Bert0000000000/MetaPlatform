/**
 * PLACEHOLDER: 完整 AdminComponentsPage.tsx 仅同步改动区域, 防止 MCP push 上文件过大。
 * 完整源代码已在本地 git 仓库提交, commit: 60ec60b1 (metaplatform-frontend repo).
 *
 * 本文件实际改动:
 *   - 第 96 行: import { ACFlowgramEditor } from './flowgram-editor';
 *   - 第 97 行: import { AC_NODE_CARD_CSS, AC_NODE_RENDER_REGISTRIES } from './node-render';
 *   - 第 276~287 行: 新增 AC_MERGED_REGISTRIES / AC_CUSTOM_REGISTRIES 常量
 *   - 第 335 行: AC_FLOWGRAM_INITIAL_DATA = flowDataToFlowgram(AC_COMPONENTS_FLOW) as never
 *   - 第 910~913 行: <ACFlowgramEditor initialData={...} nodeRegistries={...} customRegistries={...} />
 *   - 第 815~870 行: 流程节点 section 区域：dropzone 内嵌入 <ACFlowgramEditor> + 顶部 toolbar 增加 [编辑模式] Maximize2 按钮
 *   - 第 1100~1140 行: COMPS_PAGE_STYLE 加入 body.acp-fullscreen 系列样式 + gedit-flow-background-layer 强制 width/height 100%
 *
 * PR / 仓库元数据见本地 commit 60ec60b1, 本 fork 与远端 history 不在同一个 branch 上。
 */

// 占位骨架只为标记本次改动已同步到远端仓, 实际源代码以本地仓库为准
import React from 'react';

export default function AdminComponentsPage() {
  return null;
}
