[project]
name = "mate-tech-agent-team"
version = "0.1.0"
description = "Mate Platform - Agent 产品层 1.0: 超级大脑 (langgraph 任务图) + 数字员工运行时, 坐在 Ontology 之上, 租户隔离由 RLS 强制。"
requires-python = ">=3.12"
readme = "README.md"
license = { text = "Proprietary" }
authors = [{ name = "Mate Platform Team" }]
dependencies = [
    "fastapi>=0.115.0",
    "pydantic>=2.0.0",
    "mate-platform",
    "mate-clients",
    "mate-kernel",
    "langgraph>=1.2.9",
    "langgraph-checkpoint-postgres>=3.1.2",
    "psycopg[binary,pool]>=3.2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "respx>=0.21.0",
    "ruff>=0.8.0",
    "pyright>=1.1.390",
    "httpx>=0.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mate_tech_agent_team"]

[tool.uv.sources]
mate-platform = { workspace = true }
mate-clients = { workspace = true }
mate-kernel = { workspace = true }
