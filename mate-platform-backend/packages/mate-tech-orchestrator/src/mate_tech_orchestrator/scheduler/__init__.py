"""mate_tech_orchestrator.scheduler — scheduling core.

Contains the digital-employee role registry, the task dispatcher, and
the plan runner. The role registry is tenant-scoped and dynamic: roles
can be registered / unregistered at runtime (7+N digital employees).
"""
