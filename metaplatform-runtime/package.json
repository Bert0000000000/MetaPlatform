{
  "name": "metaplatform-runtime",
  "version": "0.1.0",
  "description": "Runtime image for a published MetaPlatform application — fully isolated SQLite + minimal HTTP API, no admin/auth, no outbound network.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "uuid": "^10.0.0"
  }
}
