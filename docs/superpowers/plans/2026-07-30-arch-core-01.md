
### Task 6：清理根目录重复部署源码

**Files:**
- Delete: `services/api-gateway/`（根目录副本）
- Delete: `services/auth-service/`（根目录副本）
- Modify: `mate-platform-backend/.gitignore`
- Create: `docs/active/delivery/evidence/arch-core-01-source-cleanup.md`
- Create: `mate-platform-backend/tests/architecture/test_no_duplicate_source.py`

- [ ] **Step 1: 写守恒失败测试**

`tests/architecture/test_no_duplicate_source.py`：

```python
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]


def test_root_services_only_in_workspace() -> None:
    offenders = []
    for path in (ROOT / "services").iterdir():
        if (path / "src" / path.name.replace("-", "_") / "main.py").exists():
            offenders.append(path.name)
    assert offenders == []
```

- [ ] **Step 2: 确认失败**

Run: `cd mate-platform-backend && .venv/Scripts/python.exe -m pytest tests/architecture/test_no_duplicate_source.py -q`

Expected: FAIL，列出根目录 `services/api-gateway`、`services/auth-service`。

- [ ] **Step 3: 验证冗余副本确实与 monorepo 一致**

```bash
git diff --no-index --stat services/api-gateway mate-platform-backend/services/api-gateway
git diff --no-index --stat services/auth-service mate-platform-backend/services/auth-service
```

Expected: 差异极小（仅 shell 写法和 LF/CRLF）。**若差异较大或包含业务改动，先将改动迁入 monorepo 再删除根副本，不得丢失改动。**

- [ ] **Step 4: 删除根目录副本**

```bash
cd D:\Hermes\Workspace\10_Projects\2026-07-02-MetaPlatform\.worktrees\arch-core-01
git rm -r services/api-gateway services/auth-service
```

- [ ] **Step 5: 更新 .gitignore**

在 `mate-platform-backend/.gitignore` 中追加：

```
# Services only live in the monorepo copy
/services/
```

- [ ] **Step 6: 编写清理证据**

`docs/active/delivery/evidence/arch-core-01-source-cleanup.md`：

- 删除前的源码分布；
- 删除后的源码分布；
- 与 monorepo 副本的 diff 摘要；
- 构建入口仍由 `mate-platform-backend/services/*/Dockerfile` 承担；
- 任何需要后续迁移的差异。

- [ ] **Step 7: 运行测试**

Run: `cd mate-platform-backend && .venv/Scripts/python.exe -m pytest tests/architecture -q`

Expected: PASS。

- [ ] **Step 8: 提交**

```bash
git add -A services mate-platform-backend/.gitignore docs/active/delivery/evidence/arch-core-01-source-cleanup.md tests/architecture/test_no_duplicate_source.py
git commit -m "refactor(arch): remove duplicate deployment source trees"
```

---
