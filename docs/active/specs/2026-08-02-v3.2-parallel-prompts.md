# v3.2 并行调度 prompts(4 worker)· 2026-08-02

> 版本:v1.0 · 2026-08-02
> 配套:`2026-08-01-roadmap-v3.2.md` v1.0
> 状态:**Active**(v3.2 启动 / v3.1 收尾并行)
> 修订人:需求层(TRAE)

---

## 总览:4 个 worker 并行

| Worker | 主线 | 启动任务(本周) | 后续(2-3 周) |
|---|---|---|---|
| **W1 后端主** | B1 mcp federation 真实化 | mcp federation / 外部 MCP 接入 | C1 Paimon 部署 |
| **W2 后端副** | B2 llmgw 多模态 + B3 ont SHACL | B3 ont SHACL 推理 | B2 llmgw 多模态 |
| **W3 infra** | A1 G1 kafka + A2 G5 + D1 staging | A1 G1 kafka sub-chart | A2 G5 + D1 staging 演练 |
| **W4 文档 / 收尾** | E1 CHANGELOG + E2 docs 对账 + C3 Trino | E1 CHANGELOG v3.1 + E2 docs 全面对账 | C3 Trino 协助 |

**最快 4 周完成 v3.2-α → β → γ 早期**。

---

## W1 后端主:B1 mcp federation 真实化

```text
你是 Mate Platform 的 W1(后端主),任务是 **B1: mcp federation 真实化**。
今天 2026-08-02,v3.1 GA 已实质闭环。

## 任务目标

让 `mate-tech-mcp` 的 federation 功能从"in-memory stub"升级到"真实联邦接入":
- 接受外部 MCP server 注册(name / endpoint / auth_type)
- 联邦 server 健康检查 / 状态维护
- 联邦 tool 列表聚合(从所有联邦 server 拉取)
- 联邦 tool invoke 真实 HTTP 调用(用 BearerAuth)
- 联邦 server 掉线自动重试 + DLQ

## 必读规范

- `docs/active/specs/2026-08-01-mcp-federation-spec-revision.md`(R-1 已就绪)
- `contracts/openapi/services/mcp.yaml`(7 federation endpoint spec 已就绪)
- `ADR-0014-tech-services-integration.md`(5 步接入)

## 修改文件清单

packages/mate-tech-mcp/src/mate_tech_mcp/federation/
  + clients/mcp_remote_client.py    [新建] 真实 HTTP 客户端(httpx + BearerAuth + OutgoingAuthMiddleware)
  + auth/credential_store.py        [新建] SealedSecret 引用(联邦 server 认证)
  + health/heartbeat.py              [新建] 60s 间隔健康检查
  + retry/dlq.py                     [新建] 失败重试 + DLQ(走 Outbox)

packages/mate-tech-mcp/tests/
  + test_mcp_federation_real.py      [新建] ≥ 10 cases(成功 / 401 / 503 / 超时 / 重连 / DLQ)
  + test_mcp_health_e2e.py           [新建] ≥ 5 cases(心跳 / 状态切换)

## ADR-0014 5 步 checklist

- [ ] install_auth(app) 已就绪
- [ ] require_tenant(ctx) 每个 handler 第一行
- [ ] outbox 写:联邦注册 / 状态变更 / invoke 调用
- [ ] BearerAuth + OutgoingAuthMiddleware 用于出向调用
- [ ] tests ≥ 3 cross-tenant negative

## 验收

cd mate-platform-backend/packages/mate-tech-mcp
python -m pytest tests/ -v
# 期望 ≥ 110 passed(原 95 + 新 15),0 failed

# 联邦真实调用(可选,启动 dev server + 注册 mock server)
curl -X POST http://localhost:8200/api/v1/mcp/federation/servers \
  -H "Authorization: Bearer $JWT" \
  -d '{"name": "test-mcp", "endpoint": "http://test-mcp:8080", "auth_type": "bearer"}'
# 期望 201
```

---

## W2 后端副:B3 ont SHACL + B2 llmgw 多模态

```text
你是 Mate Platform 的 W2(后端副),任务是 **B3 → B2**。
今天 2026-08-02。

## 任务目标

### Week 1:B3 ont SHACL 推理引擎

- 在 `mate-tech-ont` 已有推理引擎(InferenceEngine with SubclassRule + TransitivityRule)上
  添加 SHACL(Shapes Constraint Language)推理能力。
- 推理任务:/ont/inference/apply 接收 SHACL shapes turtle 字符串 + 现有 ontology,
  返回 validation report(违规列表 + 严重度)。
- 真实应用:在 ont POST /ont/classes 时自动跑 SHACL validation,违规则拒绝。

### Week 2-3:B2 llmgw 多模态

- 现有 llmgw 已实现 chat / chat/stream / embeddings 3 个 endpoint(已接受)。
- 新增:`POST /api/v1/llmgw/chat/multimodal`(图像 / 语音 / 视频输入,文本输出)
- 真实 provider:OpenAI gpt-4-vision / Anthropic claude-3.5-sonnet / 豆包视觉 API
- 限流 + 配额桶(复用现有 quota/)

## 必读规范

- `docs/active/specs/2026-08-01-roadmap-v3.2.md` 阶段 2 + 阶段 4
- `docs/active/specs/2026-07-31-features-backlog.md` v1.3(llmgw 多模态缺口)
- `ADR-0016-data-platform-architecture.md`(若需要数据集成)
- `ADR-0014`(5 步接入模式)

## 修改文件清单

### B3 部分(mate-tech-ont)

packages/mate-tech-ont/src/mate_tech_ont/inference/
  + shacl_engine.py              [新建] SHACL 推理引擎(基于 pyshacl 库)
  + shapes_loader.py             [新建] shapes turtle 文件加载
  + validation_report.py         [新建] 违规数据结构

packages/mate-tech-ont/src/mate_tech_ont/api/
  + shapes.py                    [新建] GET/POST /ont/shapes endpoint

packages/mate-tech-ont/tests/
  + test_shacl_engine.py         [新建] ≥ 8 cases

### B2 部分(mate-tech-llmgw)

packages/mate-tech-llmgw/src/mate_tech_llmgw/
  + multimodal/                  [新建模块]
    + providers/openai_vision.py
    + providers/anthropic_vision.py
    + providers/doubao_vision.py
    + image_processor.py
    + audio_processor.py

packages/mate-tech-llmgw/src/mate_tech_llmgw/api/
  + multimodal.py                [新建] POST /chat/multimodal

packages/mate-tech-llmgw/tests/
  + test_multimodal.py           [新建] ≥ 12 cases

## ADR-0014 5 步 checklist

- [ ] install_auth + require_tenant
- [ ] outbox 写(shacl violation event / multimodal invoke event)
- [ ] BearerAuth + OutgoingAuthMiddleware(出向调用 provider)
- [ ] tests ≥ 3 cross-tenant negative
- [ ] OpenAPI security 三段式

## 验收

cd mate-platform-backend
python -m pytest packages/mate-tech-ont/tests/ packages/mate-tech-llmgw/tests/ -v
# 期望 ≥ 8 + 12 = 20 新 passed,0 failed

# 真实多模态(可选,需 OpenAI API key)
curl -X POST http://localhost:8200/api/v1/llmgw/chat/multimodal \
  -H "Authorization: Bearer $JWT" \
  -F "image=@test.png" -F "prompt=这张图里有什么?"
# 期望 200 + 模型描述
```

---

## W3 infra:A1 G1 kafka + A2 G5 + D1 staging

```text
你是 Mate Platform 的 W3(infra),任务是 **A1 → A2 → D1**。
今天 2026-08-02。

## 任务目标

### Week 1:A1 G1 kafka sub-chart 落地

- 评估 Bitnami kafka chart(15.x)/ Confluent cp-kafka chart(7.x) 选型
- 写 `infra/helm/charts/kafka/Chart.yaml` + `values.yaml`
- KRaft 模式(无 Zookeeper)+ 持久化(PVC)
- 集成到 `infra/helm/umbrella/`(已存在 umbrella chart)
- 新建 `evidence/G1-ACCEPTANCE.md`

### Week 2:A2 G5 剩余 17 域 security 段补齐 + D1 staging

- A2:扫所有 `contracts/openapi/services/*.yaml`,确保每个 endpoint 有 security: 三段式
- D1:把 mate-platform helm chart + kafka chart + 4 个 DATA sub-chart(debezium / marquez / datahub / ge)推到真实 staging
- 端到端 smoke:数据 CDC → Iceberg → lineage → GA
- 新建 `evidence/D1-STAGING-ACCEPTANCE.md`

## 必读规范

- `docs/active/specs/2026-07-30-backend-production-readiness-design.md` §13 硬规则
- `ADR-0013-platform-event-outbox.md`(kafka 集成细节)
- `architecture-implementation.md` §1.2 服务全景
- `2026-08-01-roadmap-v3.2.md` 阶段 1

## 修改文件清单

infra/helm/charts/kafka/
  Chart.yaml                       [新建]
  values.yaml                      [新建]
  _helpers.tpl                     [新建]
  statefulset.yaml                 [新建]
  service.yaml                     [新建]
  networkpolicy.yaml               [新建]
  README.md                        [新建](helm-docs 同步)
  NOTES.txt                        [新建]

infra/helm/umbrella/Chart.yaml     [改] 把 kafka 加到 dependencies
infra/helm/umbrella/values.yaml    [改] 启用 kafka + 配置连接

contracts/openapi/services/        [G5 改] 17 域 security 三段式补齐剩余
  *.yaml                           每个 endpoint 加:
                                  security:
                                    - bearerAuth: []
                                      tenantHeader: []
                                      oidcScopes: [platform.read]

docs/active/delivery/evidence/
  G1-ACCEPTANCE.md                  [新建] kafka sub-chart 选型 + 落地
  D1-STAGING-ACCEPTANCE.md         [新建] 真实 staging 集群演练

## 验收

cd infra/helm/charts/kafka
helm lint .                          # 期望 0 errors
helm template .                       # 期望 valid yaml

cd ../umbrella
helm dependency build .               # 期望 kafka + 4 sub-charts 都 build OK
helm install test . --dry-run         # 期望 0 errors

# staging 演练(需要真实 K8s)
kubectl apply -f umbrella/            # 期望所有 pod Ready
# 端到端 smoke(参考现有 test_ga_smoke.py)
pytest ../../tests/test_staging_smoke.py -v
# 期望 ≥ 5 passed
```

---

## W4 文档 / 收尾:E1 CHANGELOG + E2 docs + C3 Trino

```text
你是 Mate Platform 的 W4(文档 / 收尾),任务是 **E1 + E2 + C3 协助**。
今天 2026-08-02。

## 任务目标

### Week 1:E1 + E2(纯文档,无需改代码)

- **E1**:`CHANGELOG.md` 追加 [v3.1] 章节,记录 7/30 GA + 7/31-8/2 全部 sub-batch
- **E2**:文档全面对账,更新以下 4 份:
  - `PROGRAM-BOARD.md` — 14 个 Delivery Batch + G1-G8 状态最终化
  - `2026-07-27-mate-platform-delivery-roadmap.md` — v1.5 → v2.0
  - `2026-07-31-features-backlog.md` — v1.3 → v2.0
  - `2026-07-31-backend-impl-backlog.md` — v1.7 → v2.0
- 新建 `docs/active/delivery/evidence/V3.1-FINAL-STATUS.md`(v3.1 收口最终状态)

### Week 2-3:C3 Trino 真实部署 + 协助

- Trino 真实部署(在 Paimon + Iceberg 已落地后)
- 协助 W3 staging 演练
- 新建 `evidence/C3-TRINO-ACCEPTANCE.md`

## 必读规范

- `2026-08-02-fix-verification.md`(v3.1 最终验收)
- `2026-08-02-g8-final-verification.md`
- `CHANGELOG.md` v3.0 / v3.2 / v3.2.1 / v3.3 已有章节格式

## 修改文件清单

CHANGELOG.md                                              [改] 追加 [v3.1] 章节

docs/active/delivery/PROGRAM-BOARD.md                      [改] 8/2 状态最终化
docs/active/specs/2026-07-27-mate-platform-delivery-roadmap.md  [改] v1.5 → v2.0
docs/active/specs/2026-07-31-features-backlog.md          [改] v1.3 → v2.0
docs/active/specs/2026-07-31-backend-impl-backlog.md       [改] v1.7 → v2.0

docs/active/delivery/evidence/
  V3.1-FINAL-STATUS.md                                    [新建] v3.1 收口最终状态
  C3-TRINO-ACCEPTANCE.md                                  [新建] Trino 部署

infra/helm/charts/trino/                                  [新建]
  Chart.yaml / values.yaml / _helpers.tpl / deployment.yaml / service.yaml /
  networkpolicy.yaml / README.md

## 验收

CHANGELOG.md [v3.1] 章节内容
  - 列出 v3.0 → v3.1 全部 sub-batch(API-GOV-01 ... GA-ACCEPTANCE + DATA-D0-D8 + TD-5/TD-6 + P0-CLOSE + P2-W2/W3/W4/W5/W6/W7 + P3-W6/W7/W8/W9 + G2/G3/G4/G5/G6/G7/G8)
  - 数字:17/17 域接入 / SPEC 命中 214/214 / 1700+ tests / 7/8 硬规则
  - 关键 commit 列表
  - 8/2 FINAL 状态

PROGRAM-BOARD.md v3.1 GA 收口矩阵
  - 9/9 核心批次 Accepted ✅
  - v3.1 增量 8/8 已闭环(G2-G8 7/8,G1 In Progress)
```

---

## 调度总结

```
Week 1:
  W1: B1 mcp federation 真实化(1 PR)
  W2: B3 ont SHACL(1 PR)
  W3: A1 G1 kafka(1 PR)
  W4: E1 CHANGELOG + E2 docs(纯 markdown)

Week 2:
  W1: C1 Paimon 部署(1 PR,需 A1 完成)
  W2: B2 llmgw 多模态(1 PR)
  W3: A2 G5 + D1 staging 演练(2 PR)
  W4: V3.1-FINAL-STATUS 文档

Week 3:
  W1: 等待
  W2: 等待
  W3: D1 staging 端到端(1 PR)
  W4: C3 Trino 协助

Week 4:
  W4: C3 Trino 完成 + 数据产品发布
```

---

## 关联文档

- `2026-08-01-roadmap-v3.2.md` v1.0 — v3.2 路线总览
- `2026-08-02-fix-verification.md` — v3.1 最终验收
- `2026-08-02-g8-final-verification.md` — G8 FINAL
- `2026-08-01-mcp-federation-spec-revision.md` — W1 规范
- `ADR-0014-tech-services-integration.md` — 5 步接入模式
- `ADR-0016-data-platform-architecture.md` — 数据平台架构

---

## 变更记录

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-08-02 | v1.0 初版(4 worker 并行调度 + 完整 prompt) | 需求层(TRAE) |