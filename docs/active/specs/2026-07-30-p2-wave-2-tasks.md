# BUSINESS-SLICES P2 Wave 2 — Task Breakdown

> 版本：v1.0 · 2026-07-30
> Worktree 分支：`codex/p2-wave-2`
> 关联：`2026-07-30-p2-wave-2-spec.md` v1.0
> 关联：`2026-07-30-p2-wave-2-checklist.md` v1.0

本文档是 SPEC §8 的可执行版本 —— 每 PR 拆成具体的 TODO 任务。**Owner review 前**用本文档做 WBS；**实施中**作为进度跟踪；**完工**后归档到 evidence。

---

## 关键路径

```
PR#11 dashboard  ──→  PR#12 apphub  ──→  PR#13 arch  ──→  PR#14 copilot
   (5 天)              (5-7 天)            (10-14 天)            (14-21 天)
```

**关键路径总时长**：5 + 7 + 14 + 21 = **47 人天 ≈ 7 周**(单团队顺序)
**乐观估计**：34 人天 ≈ 5 周
**Buffer**：2 周（跨包依赖 / spec 修订 / owner review）

---

## PR#11 — dashboard 5 步合规

**工期**：5 人天
**Owner**：待指派
**依赖**：无（main 上已有 dashboard.py）
**阻塞**：—
**阻塞下游**：PR#12 / PR#13 / PR#14（所有包都参考 dashboard 模式）

### Task 11.1 — 现状审计（0.5 天）

- [ ] 读 `packages/mate-tech-iam/src/mate_tech_iam/api/dashboard.py` 全文
- [ ] 列出 34 个 handler 的现状矩阵（已含 ctx / outbox？是否 GET-only 等）
- [ ] 列出 in-memory store 引用位置（store 改造按 §5.2）
- [ ] 评估 `_handle_*` helper 是否需要 ctx 参数
- [ ] 输出 `evidence/PR11-AUDIT.md` 提交 owner review

### Task 11.2 — 代码 5 步合规（2 天）

- [ ] module-level 加 import：
  ```python
  from mate_platform.tenancy.guards import require_tenant, assert_same_tenant
  from mate_platform.messaging import Event, OutboxWriter
  ```
- [ ] 顶部加 helper：
  ```python
  def _require_ctx_or_anonymous(request: Request) -> RequestContext | None:
      """允许 anonymous 的 ctx（仅用于 auth/login）"""
      ctx = getattr(request.state, "ctx", None)
      if ctx is None or ctx.token is None:
          raise HTTPException(401, "auth required")
      return ctx if ctx.tenant_id else None
  ```
- [ ] 14 个 GET handler 加 `request: Request` 参数 + `ctx = request.state.ctx; require_tenant(ctx)`
- [ ] 9 个写 handler 加 `request: Request` 参数 + `_require_ctx_or_anonymous` + outbox.append
- [ ] 验证 `install_auth` 已在 main.py（不重复加）

### Task 11.3 — 测试新增（1.5 天）

- [ ] `tests/test_dashboard.py` 写 6 happy-path（含 outbox append mock 检查）
- [ ] `tests/test_dashboard_tenant_integration.py` 写 5 case（wrong-tenant / missing-scope / no-tenant / auth-login-anonymous / settings-put-emits-outbox）
- [ ] 用 `TestClient` + 自定义 header 注入 tenant_id（参考 `test_app_kb_tenant_integration.py`）
- [ ] 验证 `pytest packages/mate-tech-iam/tests/ -v` ≥ 9 pass

### Task 11.4 — CI / PR 收口（1 天）

- [ ] `ruff check packages/mate-tech-iam/` 净增 < 30
- [ ] `python scripts/ci/forbid_skip_tests.py` exit 0
- [ ] `pytest infra/tests -q` 回归全绿
- [ ] `pytest packages/mate-app-kb/tests/ packages/mate-tech-msg/tests/ packages/mate-tech-obs/tests/ -q` 回归全绿
- [ ] commit 信息包含 `ADR-0014` + `Ref: 2026-07-30-p2-wave-2-spec.md §4.1`
- [ ] 开 PR → owner review → squash merge → 关闭

---

## PR#12 — apphub 新包

**工期**：5-7 人天
**Owner**：待指派
**依赖**：PR#11 合入（参考 dashboard 模式）
**阻塞下游**：PR#13（arch 也参考 apphub 包模板）

### Task 12.1 — 包脚手架（0.5 天）

- [ ] `packages/mate-app-hub/pyproject.toml` 创建
- [ ] `packages/mate-app-hub/src/mate_app_hub/__init__.py`
- [ ] `packages/mate-app-hub/src/mate_app_hub/api/__init__.py`
- [ ] `packages/mate-app-hub/src/mate_app_hub/repositories/__init__.py`
- [ ] `packages/mate-app-hub/tests/__init__.py`
- [ ] `packages/mate-app-hub/tests/conftest.py` 含 `client` / `auth_headers_acme` / `auth_headers_globex` fixture
- [ ] `mate-platform-backend/pyproject.toml` workspace 加 `packages/mate-app-hub`
- [ ] `uv sync` 验证

### Task 12.2 — main.py（0.5 天）

- [ ] `packages/mate-app-hub/src/mate_app_hub/main.py` 写：
  ```python
  from fastapi import FastAPI
  from mate_platform.auth import install_auth
  from mate_app_hub.api.app import router

  def create_app() -> FastAPI:
      app = FastAPI(title="mate-app-hub")
      install_auth(app)
      app.include_router(router)
      return app

  app = create_app()  # for `uvicorn mate_app_hub.main:app`
  ```

### Task 12.3 — repositories/in_memory.py（1 天）

- [ ] 5 个 dataclass：`ApphubApp` / `ApphubGroup` / `ApphubModule` / `ApphubPage` / `ApphubTemplate`
- [ ] 多 tenant dict：`_STORE: dict[str, dict[str, dict[str, Any]]] = {}`
- [ ] seed fixture 函数 `_seed_apps(tenant_id)`: 15 apps + 3 groups + 8 modules + 12 pages + 6 templates
- [ ] 5 公共函数：`list_apps(tenant_id)` / `list_groups(tenant_id)` / `list_modules(tenant_id)` / `list_pages(tenant_id)` / `list_templates(tenant_id)`
- [ ] 第一次调用时 lazy seed

### Task 12.4 — api/app.py（1 天）

- [ ] router = `APIRouter(prefix="/api/v1/apphub", tags=["apphub"])`
- [ ] 5 GET handler：
  ```python
  @router.get("/apps", response_model=list[ApphubAppSchema])
  async def list_apps(request: Request):
      ctx = request.state.ctx
      require_tenant(ctx)
      return await repositories.list_apps(ctx.tenant_id)
  ```
- [ ] Pydantic schema：`ApphubAppSchema` 等 5 个

### Task 12.5 — clients.py（0.5 天）

- [ ] `AsyncApphubClient` class（预留 P2 W3 给 arch / kb 调用）
- [ ] `get_apps()`, `get_modules()` 等用 respx mock-able 接口
- [ ] 写 docstring 说明 stub 用途

### Task 12.6 — 测试（1.5 天）

- [ ] `tests/test_app_hub.py`：5 happy-path
  - [ ] `test_list_apps_ok` （returns ≥15 entries）
  - [ ] `test_list_groups_ok`
  - [ ] `test_list_modules_ok`
  - [ ] `test_list_pages_ok`
  - [ ] `test_list_templates_ok`
- [ ] `tests/test_app_hub_tenant_integration.py`：4 case
  - [ ] `test_wrong_tenant_isolation` （acme vs globex 不互见）
  - [ ] `test_missing_scope_403`
  - [ ] `test_no_tenant_400`
  - [ ] `test_auth_login_anonymous_ok` （如有 login endpoint）

### Task 12.7 — PR 收口（1 天）

- [ ] `ruff check packages/mate-app-hub/` 净增 < 30
- [ ] pre-commit hooks 全 exit 0
- [ ] 回归测试
- [ ] commit 信息包含 `ADR-0014` + `Ref: 2026-07-30-p2-wave-2-spec.md §4.2`
- [ ] 开 PR → owner review → squash merge → 关闭

---

## PR#13 — arch 新包

**工期**：10-14 人天
**Owner**：待指派
**依赖**：PR#12 合入（参考 apphub 包模板）
**阻塞下游**：PR#14（copilot 用 arch 的 ontology 代理）

### Task 13.1 — 包脚手架 + main.py（0.5 天）

- [ ] 同 PR#12 模板，目录换成 `packages/mate-app-arch/`
- [ ] `pyproject.toml` 加 dependency `mate-tech-ont`（代理调用 ontology）
- [ ] `main.py` 同模板

### Task 13.2 — repositories/in_memory.py（3 天）

- [ ] 14 个 dataclass（Application / BusinessProcess / Capability / DataAsset / DataEntity / DataFlow / DataStandard / Deployment / GovernancePrinciple / Org / Role / TechStack / TechnologyComponent / ValueStream）
- [ ] Capability 加 `parent_id` field 支持 tree
- [ ] Org 加 `parent_id` field 支持 tree
- [ ] 多 tenant dict
- [ ] seed fixture：
  - [ ] ≥20 applications（kb / rag / llmgw / mcp / obs / msg / ont / agent / arch / copilot / dashboard / dw / a2a / wfe / data + 5 个虚构）
  - [ ] ≥15 capabilities（含 tree structure：3 层 parent/child）
  - [ ] ≥10 data-assets
  - [ ] ≥5 orgs（含 tree structure：2 层）
  - [ ] ≥5 governance principles
  - [ ] ≥10 tech-stack entries
  - [ ] ≥5 deployments / infrastructures
- [ ] 27 公共查询函数
- [ ] `value-streams` 静态 DAG（每个 stream 是有序 list of capability_ids）
- [ ] `impact-analysis` 用 BFS from `node_id` over capability tree

### Task 13.3 — api/app.py（3 天）

- [ ] router = `APIRouter(prefix="/api/v1/arch", tags=["arch"])`
- [ ] 27 GET handler，每行 `require_tenant(ctx)`
- [ ] 14 个 Pydantic schema（Application / BusinessProcess / Capability / DataAsset / DataEntity / DataFlow / DataStandard / Deployment / GovernancePrinciple / Org / Role / TechStack / TechnologyComponent / ValueStream / TechRadarEntry）

### Task 13.4 — clients.py（1 天）

- [ ] `AsyncArchClient` class
- [ ] `mate_tech_ont` 代理调用（concepts search → arch applications）
- [ ] `mate_tech_iam` org 调用（org 表 → arch orgs）
- [ ] 用 `OutgoingAuthMiddleware` 包 httpx
- [ ] 失败 fallback to in-memory

### Task 13.5 — 测试（2 天）

- [ ] `tests/test_app_arch.py`：5 happy-path
  - [ ] `test_list_applications_ok`
  - [ ] `test_get_capabilities_tree_ok` （含 ≥3 层嵌套）
  - [ ] `test_get_data_assets_catalog_ok`
  - [ ] `test_get_orgs_tree_ok`
  - [ ] `test_get_governance_principles_ok`
  - [ ] `test_get_value_streams_ok` （DAG 结构）
  - [ ] `test_get_impact_analysis_ok` （BFS 结果）
- [ ] `tests/test_app_arch_tenant_integration.py`：3 negative + 1 isolation

### Task 13.6 — PR 收口（1.5 天）

- [ ] `ruff check packages/mate-app-arch/` 净增 < 30
- [ ] pre-commit hooks 全 exit 0
- [ ] 回归测试
- [ ] commit 信息包含 `ADR-0014` + `Ref: 2026-07-30-p2-wave-2-spec.md §4.3`
- [ ] 开 PR → owner review → squash merge → 关闭

---

## PR#14 — copilot 新包

**工期**：14-21 人天
**Owner**：待指派
**依赖**：PR#13 合入（用 arch 的 ontology 代理）
**阻塞下游**：—

### Task 14.1 — 包脚手架 + main.py（0.5 天）

- [ ] 同 PR#13 模板，目录换成 `packages/mate-app-copilot/`
- [ ] `pyproject.toml` 加 `sqlparse>=0.5`

### Task 14.2 — repositories/in_memory.py（2 天）

- [ ] 7 dataclass：`Conversation` / `Query` / `Plan` / `Intent` / `Template` / `Action` / `CodeGeneration`
- [ ] 多 tenant dict
- [ ] seed fixture：≥10 conversations + ≥20 queries + ≥5 plans + ≥5 intents + ≥5 templates + ≥10 actions

### Task 14.3 — llm/stub_provider.py（1 天）

- [ ] `embeddings(texts: list[str]) -> list[list[float]]`：
  - [ ] 用 hashlib 算 sha256 → 取前 32 字节 → 归一化 → 拼成 1536 维向量
  - [ ] 返固定种子让 deterministic
- [ ] `chat(messages: list[dict]) -> str`：
  - [ ] 返固定 fixture + 拼接 message content 第一句
- [ ] `generate_sql(nl_prompt: str, tables: list[str]) -> str`：
  - [ ] 返 `SELECT * FROM {tables[0]}` + 占位 stub
- [ ] `audit_sql(sql: str) -> list[Finding]`：
  - [ ] sqlparse 解析 + 检查 SELECT * / DELETE without WHERE / JOIN 无 ON / DROP
  - [ ] 返 list of `(rule_id, severity, message)`

### Task 14.4 — clients.py（1 天）

- [ ] `AsyncCopilotClient` class
- [ ] `mate_tech_llmgw` 代理（embeddings）
- [ ] `mate_app_kb` 代理（search）
- [ ] `mate_tech_ont` 代理（concepts / graph）
- [ ] `mate_tech_agent` 代理（orchestration）
- [ ] 用 `OutgoingAuthMiddleware`

### Task 14.5 — api/app.py（6 天）

按 17 个功能簇逐簇写：

- [ ] **簇 0 Auth** (0.5d)：1 endpoint `auth/login` POST → stub token
- [ ] **簇 1 A2A** (0.5d)：2 endpoint 返 501 Not Implemented
- [ ] **簇 2 Actions** (1d)：3 endpoint
  - [ ] `actions` GET → list fixture actions
  - [ ] `actions/match` POST → match by intent
  - [ ] `actions/execute` POST → execute + outbox
- [ ] **簇 3 Analysis (SQL Copilot)** (2d)：4 endpoint
  - [ ] `analysis/explain-sql` GET → sqlparse + stub explanation
  - [ ] `analysis/audit-sql` POST → sqlparse + audit_sql findings
  - [ ] `analysis/generate-sql` POST → stub LLM generate
  - [ ] `analysis/execute-sql` POST → 拒绝 non-SELECT + dry-run result
- [ ] **簇 4 Chat** (1d)：1 endpoint
  - [ ] `chat/multimodal/upload` POST → stub embed + minio stub + outbox
- [ ] **簇 5 Code** (0.5d)：1 endpoint `code` GET → static
- [ ] **簇 6 Conversations** (0.5d)：1 endpoint
- [ ] **簇 7 Datasources** (0.5d)：1 endpoint
- [ ] **簇 8 Generate** (1.5d)：4 endpoint
  - [ ] `generate/dashboard` POST → JSON skeleton
  - [ ] `generate/explain-code` POST → markdown stub
  - [ ] `generate/form` POST → form spec JSON
  - [ ] `generate/process` GET → BPMN stub
  - [ ] `generate/review-code` POST → review markdown
- [ ] **簇 9 Knowledge-bases** (0.5d)：1 endpoint 代理 mate-app-kb
- [ ] **簇 10 Models** (0.5d)：1 endpoint
- [ ] **簇 11 Ontology** (1d)：3 endpoint 代理 mate-tech-ont fallback in-memory
- [ ] **簇 12 Plans** (0.5d)：1 endpoint
- [ ] **簇 13 Queries** (1d)：2 endpoint（execute POST + history GET）
- [ ] **簇 14 Scheduling** (2d)：5 endpoint（intent detect / plan generate / employees match / execution start / intents list / templates list）
- [ ] **簇 15 Search** (0.5d)：1 endpoint 代理 mate-app-kb
- [ ] **簇 16 Root** (0.5d)：1 endpoint `GET /copilot` → service info

总计 27 GET + 6 POST = 33 endpoint，跨 17 簇。

### Task 14.6 — 测试（3 天）

- [ ] `tests/test_app_copilot.py`：8 happy-path
  - [ ] `test_auth_login_ok`
  - [ ] `test_actions_match_ok`
  - [ ] `test_analysis_explain_sql_ok`
  - [ ] `test_chat_multimodal_upload_ok`
  - [ ] `test_ontology_concepts_search_ok`
  - [ ] `test_queries_execute_ok`
  - [ ] `test_scheduling_intent_detect_ok`
  - [ ] `test_search_kb_proxy_ok`
- [ ] `tests/test_app_copilot_tenant_integration.py`：5 case
  - [ ] wrong-tenant / missing-scope / no-tenant / isolation / a2a-stub-501

### Task 14.7 — PR 收口 + 最终 evidence（1.5 天）

- [ ] `ruff check packages/mate-app-copilot/` 净增 < 50
- [ ] pre-commit hooks 全 exit 0
- [ ] 回归测试
- [ ] commit 信息包含 `ADR-0014` + `Ref: 2026-07-30-p2-wave-2-spec.md §4.4`
- [ ] 开 PR → owner review → squash merge → 关闭
- [ ] 写 `evidence/P2-W2-ACCEPTANCE.md`：
  - [ ] 4 包 / 99 endpoint 全部 5 步合规
  - [ ] ≥19 happy-path 测试 pass
  - [ ] ≥12 tenant negative pass
  - [ ] 0 raw SQL / 0 bare httpx / 0 fallback / 0 skip
  - [ ] pyright strict exit 0
  - [ ] ruff check 净增 < 200
- [ ] 更新 `PROGRAM-BOARD.md` v3.1 章节：BUSINESS-SLICES P2 W2 = Accepted
- [ ] 更新 `delivery-roadmap.md` v1.3 附录 B.6：实际工时 + 净改动
- [ ] Slack 通知 owner review 下一个 P2 W3 起点

---

## 工时总表

| PR | 工期 (人天) | 风险 buffer | 关键风险 |
|---|---:|---:|---|
| PR#11 dashboard | 5 | 1 | 改 34 个 handler 引入 regression |
| PR#12 apphub | 5-7 | 1 | 新建包模式探索 |
| PR#13 arch | 10-14 | 2 | 27 个 fixture + tree/DAG 算法 |
| PR#14 copilot | 14-21 | 3 | 33 endpoint + 17 簇 + LLM stub |
| **合计** | **34-47** | **7** | — |
| **含 buffer** | **41-54 人天** | — | **6-8 周（单团队顺序）** |

---

## 实施期跟踪模板

每周一更新状态（参考 `delivery-roadmap.md` §8 weekly 模板）：

```markdown
### Week 2026-XX-XX (P2 W2)

**已完成（合入主干）**：
- [ ] PR#11 dashboard 5 步合规：…（进度）

**进行中**：
- [ ] PR#12 apphub 新包：…（%）

**阻塞**：
- 无

**本周风险**：
- …

**下周计划**：
- …
```

---

## 关联文档

- `docs/active/specs/2026-07-30-p2-wave-2-spec.md` v1.0
- `docs/active/specs/2026-07-30-p2-wave-2-checklist.md` v1.0
- `docs/active/specs/2026-07-30-per-app-integration-checklist.md` v1.0
- `docs/active/specs/2026-07-30-backend-impl-backlog.md` v1.0
- `docs/active/delivery/evidence/BUSINESS-SLICES-ACCEPTANCE.md`（evidence 模板）

---

## 变更记录

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-07-30 | 初版 v1.0 | TRAE 在 `codex/p2-wave-2` 分支起草 |
