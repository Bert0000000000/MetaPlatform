apiVersion: v1
kind: Service
metadata:
  name: metaplatform-frontend
  namespace: metaplatform
  labels:
    app: metaplatform-frontend
spec:
  type: ClusterIP
  selector:
    app: metaplatform-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP