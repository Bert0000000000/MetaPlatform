# Secret for MetaPlatform API (DO NOT commit real values)
# Use sealed-secrets / external-secrets / vault in real clusters
apiVersion: v1
kind: Secret
metadata:
  name: metaplatform-api-secret
  namespace: metaplatform
type: Opaque
stringData:
  JWT_SECRET: "CHANGE_ME_use_sealed_secrets_or_external_secrets_operator"
  DATABASE_URL: "postgres://meta:CHANGE_ME@metaplatform-postgres:5432/metaplatform"
  LLM_API_KEY: "sk-CHANGE_ME"
  ANTHROPIC_API_KEY: "sk-ant-CHANGE_ME"
  NEO4J_PASSWORD: "CHANGE_ME"
  MINIO_ACCESS_KEY: "CHANGE_ME"
  MINIO_SECRET_KEY: "CHANGE_ME"
  CLICKHOUSE_PASSWORD: "CHANGE_ME"