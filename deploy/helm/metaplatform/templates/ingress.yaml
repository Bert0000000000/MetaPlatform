{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "metaplatform.fullname" . }}
  labels:
    {{- include "metaplatform.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- with .Values.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .path "/api" }}
                name: {{ include "metaplatform.fullname" $ }}-api
                {{- else }}
                name: {{ include "metaplatform.fullname" $ }}-frontend
                {{- end }}
                port:
                  number: 80
          {{- end }}
    {{- end }}
{{- end }}