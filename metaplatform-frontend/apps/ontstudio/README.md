# APP-ONTSTUDIO

当前 Ontology Studio 前端代码位于项目根目录 `APP-ONTSTUDIO/`。

该目录为 monorepo 占位符，待后续按重构模式迁入。

迁入前请先参考：
- `apps/portal/`：monorepo 应用最小模板
- `apps/dashboard/`：完整接入 `@mate/shared` 的示例
- `metaplatform-frontend/packages/shared/`：统一共享包
