server:
  port: 8201

spring:
  application:
    name: tech-ont
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
