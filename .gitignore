# Build outputs
**/target/
**/*.class
**/*.jar
**/*.war
**/dist/

# Node
**/node_modules/
**/package-lock.json
**/.vite/
**/*.tsbuildinfo

# Backend runtime
metaplatform-api/data/
metaplatform-api/backups/

# Published-app snapshot files (one sqlite per published app; created
# at publish time and re-populated on each re-publish. Never commit
# them — they're test output and runtime artifacts).
data/published/

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
# Trae IDE AI agent generated logs (cache/debug output)
.*.log

# Local Node runtime (portable binaries)
tools/node-v22/

# Local Maven runtime (portable binaries)
tools/maven/

# RuoYi-Vue-Pro source code (large, cloned on demand)
deploy/ruoyi-build/ruoyi-vue-pro/

# Verification screenshots / logs
verify-*.png
docs/artifacts/verification/screenshots/
docs/artifacts/verification/logs/
*.err
*.out

# Maven wrapper
!.mvn/wrapper/maven-wrapper.jar

# Python cache
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/

# SQLite local dev databases
*.db

# Pytest cache
.pytest_cache/
# pytest 临时目录重定向（规避 %TEMP%\pytest-of-<user> 的 ACL 损坏 WinError 5）
.pytest-tmp/

# Build artifacts
BOOT-INF/
META-INF/

# Vite generated config artifacts
*.vite.config.d.ts

# Local .env files
.env
.env.bak-*
.env.local
.env.*.local

# Temporary build context (workaround for WSL2 + ACL pytest_cache locks)
.tmp-build-context/

# Temporary scripts and JSON outputs
docs/006-TMP/*.json

# Playwright E2E auth state (contains session tokens — never commit)
metaplatform-frontend/tests/e2e/.auth/

# Local Claude config (not project source)
.claude/

# Local skills lock (machine-specific)
skills-lock.json
.agents/
docs/006-TMP/*.txt
docs/006-TMP/jwt.txt
# Temporary screenshots and images produced during verification
docs/006-TMP/*.png
docs/006-TMP/*.jpg
docs/006-TMP/*.jpeg
docs/006-TMP/*.gif
docs/006-TMP/*.webp
docs/006-TMP/backend-logs/
docs/006-TMP/frontend-logs/
# Verification screenshots generated in app directories
**/multimodal-*.png
**/tmp-*.png
**/tmp-*.html
**/tmp-*.json
**/tmp-*.py
# Design drafts
metaplatform-design-draft/

# === 2026-07-25 noise cleanup (see git log) ===

# Local Maven repository caches (developers run mvn locally; never commit)
**/.m2/

# Build / test output files
**/test-output.txt

# Browser console IIFE debug scripts (paste-into-DevTools one-offs)
eval_*.js

# FlowGram dev/verification screenshots
flowgram-*.png
fullscreen-*.png
shots/
zoom-shots/

# Codex / Claude session caches and uploads
.cache/
.uploads/

# Local Maven classpath snapshots (machine-specific absolute paths)
**/classpath.txt

.venv/
.wheels/
build-logs/
*.bat
.wheels-*
.uv-cache/
.coverage
.coverage.*
htmlcov/
.DS_Store
Thumbs.db
# Trae AI agent caches
.traae/
# Backup
*.orig

.worktrees/

# Generated FastAPI runtime schemas
mate-platform-backend/contracts/runtime/*.json
!mate-platform-backend/contracts/runtime/.gitkeep

# Docker build / recovery artifacts (2026-08-03 cleanup)
.tmp-build-context/
.tmp-build-logs/
.tmp-build-logs2/
.tmp-build-logs3/
.tmp-*.py
.tmp-*.sh
.tmp-commit-v*.py
.tmp-gh-push*.py
.tmp-git-status.py

# Test output artifacts
test-results/
metaplatform-frontend/apps/web/tests/e2e/.auth/
metaplatform-frontend/apps/web/tests/e2e/.artifacts/
tests/e2e/.artifacts/
tests/e2e/.auth/
tests/e2e/screenshots/
