server:
  port: 8601

spring:
  application:
    name: tech-msg
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
